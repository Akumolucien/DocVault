// config.js
// This is the plain-JS equivalent of a .env file.
// Vite/React projects read .env automatically via import.meta.env —
// a plain HTML/CSS/JS site has no build step, so the browser can't read
// a real .env file. Instead, we keep the same values here as constants.
//
// Fill these in once your API Gateway endpoint is ready.

const API_URL = "https://p63ig1syq0.execute-api.us-east-1.amazonaws.com/dev";
const API_KEY = "4kpYc153Ti4J5eATwf5hP9ietZnvI7N32wr0hA0r";
